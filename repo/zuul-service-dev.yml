spring:
  profiles:
    active: dev
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
  rabbitmq:
    host: 192.168.64.128
    port: 5672
    username: guest
    password: guest
zuul:
  routes:
#    service-a:
#      path: /service/**
#      serviceId: ribbon-service
    # 针对如上配置的简洁写法
    ribbon-service: /service/**
management:
 security:
  enabled: false
